{
  "name": "claude-telegram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Telegram control for Claude Code running unattended in a beam",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/main.js",
    "dev": "tsc -p . && node dist/main.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.273"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
